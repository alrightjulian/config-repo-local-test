spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-application
  config:
    import: http://localhost:8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/